import React from 'react';

const Logo = (props) => {
  return (
    <img
      alt="Logo"
      // src="/static/logo.svg"
      src="/static/logo-aidpi-black.png"
      {...props}
    />
  );
};

export default Logo;
